numpy>=1.24
pandas>=2.0
yfinance>=0.2.40
requests>=2.31
python-dotenv>=1.0
pytest>=7.4
# Optional research OHLCV via TWS/Gateway (IBKR_ENABLED=1):
#   pip install ib_insync
# ib_insync is not a hard dependency — desk runs without it.